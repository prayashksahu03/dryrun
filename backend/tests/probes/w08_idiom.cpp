#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <set>
#include <deque>
#include <queue>
#include <stack>
#include <algorithm>
#include <utility>
#include <numeric>
using namespace std;
int main(){ string s="abc"; s.push_back('d'); s.pop_back(); cout<<s.substr(1)<<s.find('b')<<s.c_str(); cout<<"\n"; return 0; }
